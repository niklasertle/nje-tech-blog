<h2>Your Previous Posts</h2>
{{#each posts as |post|}}
<div>
    <div>
        <a href='/post/{{id}}'>{{title}}</a>
        <p>By: {{user.name}}</p>
        <p>On: {{format_date createdAt}}</p>
    </div>
    <div>
        <p>{{description}}</p>
    </div>
</div>
{{/each}}
